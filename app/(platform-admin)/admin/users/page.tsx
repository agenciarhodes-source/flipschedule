import {unstable_noStore as noStore} from "next/cache";import {getPlatformContext} from "@/lib/auth/platform-context";import {getPrismaClient} from "@/lib/db";import {PlatformAdminReader} from "@/domains/infrastructure/platform/readers";
export const dynamic="force-dynamic";export default async function Page(){noStore();const context=await getPlatformContext();const data=await new PlatformAdminReader(getPrismaClient()).users(context);const rows:readonly {id:string}[]=Array.isArray(data)?data:(data as {webhooks:readonly {id:string}[];messages:readonly {id:string}[]}).webhooks.concat((data as {messages:readonly {id:string}[]}).messages);return <><h1 className="font-display text-3xl">Usuários</h1><p className="mt-2 text-sm text-ink-muted">Dados reais, paginados e sanitizados. Nenhum conteúdo clínico ou payload é carregado.</p><div className="mt-6 overflow-x-auto rounded-lg border border-line"><table className="w-full min-w-[720px] text-left text-sm"><thead><tr><th className="p-3">Registro sanitizado</th><th className="p-3">Estado</th></tr></thead><tbody>{rows.length===0?<tr><td className="p-6 text-ink-muted" colSpan={2}>Nenhum registro encontrado.</td></tr>:rows.map((row)=><tr className="border-t border-line" key={row.id}><td className="p-3 font-mono text-xs">{row.id}</td><td className="p-3"><pre className="max-w-4xl whitespace-pre-wrap text-xs">{JSON.stringify(row,(_key,value)=>value instanceof Date?value.toISOString():value,2)}</pre></td></tr>)}</tbody></table></div></>}

export function DataTable({ label, headings, children }: Readonly<{ label: string; headings: readonly string[]; children: React.ReactNode }>) { return <div role="region" aria-label={`${label} — deslize horizontalmente para ver todas as colunas`} tabIndex={0} className="overflow-x-auto overscroll-x-contain rounded-lg border border-line focus-visible:ring-2 focus-visible:ring-primary"><table className="w-full min-w-[680px] text-left text-sm"><caption className="sr-only">{label}</caption><thead className="bg-bg-elev font-mono text-[10px] uppercase tracking-wider text-ink-dim"><tr>{headings.map((heading, index) => <th scope="col" key={heading} className={index === 0 ? "p-4" : "px-3 py-4"}>{heading}</th>)}</tr></thead><tbody className="divide-y divide-line bg-bg-alt">{children}</tbody></table></div>; }

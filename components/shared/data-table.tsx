export function DataTable({ label, headings, children }: Readonly<{ label: string; headings: readonly string[]; children: React.ReactNode }>) { return <div className="overflow-x-auto rounded-lg border border-line"><table aria-label={label} className="w-full min-w-[680px] text-left text-sm"><thead className="bg-bg-elev font-mono text-[10px] uppercase tracking-wider text-ink-dim"><tr>{headings.map((heading, index) => <th key={heading} className={index === 0 ? "p-4" : "px-3 py-4"}>{heading}</th>)}</tr></thead><tbody className="divide-y divide-line bg-bg-alt">{children}</tbody></table></div>; }

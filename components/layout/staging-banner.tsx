import { isStagingRuntime } from "@/lib/runtime/config";
import { isSyntheticPilotRuntime } from "@/domains/pilot/data-policy";
export const STAGING_PUBLIC_MARKER = "AMBIENTE DE HOMOLOGAÇÃO";
export function StagingBanner({env=process.env,tenantSlug}:{env?:Record<string,string|undefined>;tenantSlug?:string}){if(!isStagingRuntime(env))return null;const pilot=isSyntheticPilotRuntime(env);const support=env.NEXT_PUBLIC_SUPPORT_EMAIL?.trim(),feedback=env.NEXT_PUBLIC_PILOT_FEEDBACK_URL?.trim();return <aside role="status" aria-label="Aviso de ambiente de homologação" className="sticky top-0 z-50 bg-amber-300 px-3 py-2 text-center text-xs font-semibold text-black sm:text-sm"><strong>{pilot?`${STAGING_PUBLIC_MARKER} — PILOTO: UTILIZE SOMENTE DADOS SINTÉTICOS`:`${STAGING_PUBLIC_MARKER} — USE APENAS DADOS FICTÍCIOS`}</strong>{pilot&&<span className="ml-2">Staging{tenantSlug?` · tenant ${tenantSlug}`:""} · nenhum dado real deve ser inserido.{support&&<> · Suporte: <a className="underline" href={`mailto:${support}`}>{support}</a></>}{feedback&&<> · <a className="underline" href={feedback} rel="noreferrer">Enviar feedback</a></>}</span>}</aside>}

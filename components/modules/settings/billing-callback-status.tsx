import Link from "next/link";
export function BillingCallbackStatus({tenantSlug,state}:{tenantSlug:string;state:"sucesso"|"cancelado"|"expirado"}){return <main className="mx-auto max-w-xl space-y-4 p-8"><h1 className="text-2xl font-semibold">{state==="sucesso"?"Estamos confirmando o pagamento":state==="cancelado"?"Checkout cancelado":"Checkout expirado"}</h1><p className="text-muted-foreground">Esta página não confirma pagamento nem libera acesso. O status seguro será atualizado por webhook ou reconciliação.</p><div className="flex gap-3"><Link className="rounded-md border px-4 py-2" href={`/${tenantSlug}/configuracoes/assinatura/checkout/${state}`}>Atualizar status</Link><Link className="rounded-md border px-4 py-2" href={`/${tenantSlug}/configuracoes/assinatura`}>Voltar à assinatura</Link></div></main>}

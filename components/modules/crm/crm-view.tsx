import { Filter } from "lucide-react";
import { demoLeads,demoLeadStages } from "@/domains/demo";
import { formatCompactCurrency } from "@/lib/formatting/currency";
import { PageHeader } from "@/components/shared/page-header";
export function CrmView(){return <div className="mx-auto max-w-7xl space-y-7"><PageHeader eyebrow="Relacionamento · Funil" title="CRM" description="Acompanhe oportunidades fictícias por etapa; movimentações serão implementadas em fase posterior."/><div className="flex justify-end"><button className="inline-flex min-h-10 items-center gap-2 rounded-md border border-line px-4 text-sm text-ink-muted"><Filter size={15}/>Filtros</button></div><section className="grid gap-4 overflow-x-auto pb-3 md:grid-cols-2 xl:grid-cols-4" aria-label="Funil de leads">{demoLeadStages.map(stage=>{const leads=demoLeads.filter(l=>l.stage===stage.id);return <div key={stage.id} className="min-w-64 rounded-lg bg-bg-alt p-3"><header className="mb-3 flex items-center justify-between"><h2 className="font-mono text-xs uppercase tracking-wider text-ink-muted">{stage.label}</h2><span aria-label={`${leads.length} leads`} className="rounded-full bg-bg-elev px-2 py-0.5 font-mono text-xs">{leads.length}</span></header><div className="space-y-3">{leads.map(l=><article key={l.id} className="rounded-md border border-line bg-bg-elev p-4"><h3 className="font-medium">{l.name}</h3><p className="mt-1 text-xs text-ink-muted">{l.source}</p><p className="mt-4 font-display text-2xl">{formatCompactCurrency(l.estimatedValueCents)}</p></article>)}</div></div>})}</section></div>}

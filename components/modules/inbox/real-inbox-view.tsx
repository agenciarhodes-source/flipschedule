import type {ConversationListItem} from "@/domains/application/view-models";import {PageHeader} from "@/components/shared/page-header";import {EmptyState} from "@/components/shared/empty-state";import {MessageSquare} from "lucide-react";
export function RealInboxView({items}:{items:ConversationListItem[]}){return <div className="mx-auto max-w-[1440px] space-y-6"><PageHeader eyebrow="Atendimento" title="Inbox" description="Conversas persistidas; envios externos permanecem pendentes até processamento por integração oficial."/>{items.length===0?<EmptyState icon={MessageSquare} title="Nenhuma conversa" description="Crie uma conversa interna ou aguarde uma mensagem persistida."/>:<div className="grid gap-3">{items.map(x=><article className="card-surface p-4" key={x.id}><div className="flex justify-between"><strong>{x.contact.name}</strong><span>{x.channel} · {x.status}</span></div><p className="text-sm text-ink-muted">{x.preview??"Sem prévia disponível"}</p><small>{x.unreadCount} não lida(s)</small></article>)}</div>}<p className="text-sm text-ink-muted">Prioridade, responsável e tags persistentes: Ainda não disponível.</p></div>}

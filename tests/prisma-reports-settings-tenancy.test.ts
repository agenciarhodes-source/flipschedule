import {readFileSync} from "node:fs";
import {describe,expect,it} from "vitest";
describe("reports and organization security contract",()=>{const source=readFileSync("domains/infrastructure/prisma/reports-settings.ts","utf8"),publicService=readFileSync("domains/infrastructure/prisma/treatment-inbox-services.ts","utf8");it("scopes every report and settings query to trusted context",()=>{expect(source).toContain("tenantId=this.context.tenantId");expect(source).toContain("id:this.context.tenantId");expect(source).not.toMatch(/tenantId:\s*(input|query|form)/)});it("allows organization writes only to authorized roles and audits them",()=>{expect(source).toContain('["OWNER","MANAGER"]');expect(source).toContain('action:"tenant.settings.update"');expect(source).toContain("actorMembershipId:this.context.membershipId")});it("keeps raw public tokens out of paths and query strings",()=>{expect(publicService).toContain("/plano#token=");expect(publicService).not.toContain("/plano/${token}");expect(readFileSync("components/public-plan/public-plan-from-fragment.tsx","utf8")).toContain("history.replaceState")})});

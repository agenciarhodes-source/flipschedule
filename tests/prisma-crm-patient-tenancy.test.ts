import { readFileSync } from "node:fs";import { describe,expect,it } from "vitest";
describe("CRM and patient tenant isolation",()=>{const readers=readFileSync("domains/infrastructure/prisma/crm-patient-readers.ts","utf8");const services=readFileSync("domains/infrastructure/prisma/crm-patient-services.ts","utf8");it("scopes readers and ID lookups with trusted context",()=>{expect(readers.startsWith('import "server-only";')).toBe(true);expect(readers.match(/tenantId:this\.context\.tenantId/g)?.length).toBeGreaterThanOrEqual(12);expect(readers).toContain('where:{id,tenantId:this.context.tenantId}')});it("validates relations, audits and converts atomically",()=>{expect(services.startsWith('import "server-only";')).toBe(true);expect(services).not.toMatch(/tenantId:\s*(input|form|query)/);expect(services).toContain('actorMembershipId:this.context.membershipId');expect(services).toContain('isolationLevel:"Serializable"');expect(services).toContain('patientAttribution.create')});it("keeps demo routes on their fixture views",()=>{expect(readFileSync("app/(demo)/demo/crm/page.tsx","utf8")).toContain("CrmView");expect(readFileSync("app/(demo)/demo/pacientes/page.tsx","utf8")).toContain("PatientsView")})});

import { PrismaClient } from "../generated/prisma/client";import { PrismaNeon } from "@prisma/adapter-neon";const url=process.env.DATABASE_URL;if(!url)throw new Error("DATABASE_URL_REQUIRED");if(/prod(uction)?/i.test(url)&&process.env.CONFIRM_PRODUCTION_READONLY!=="yes")throw new Error("PRODUCTION_DATABASE_DENIED");const prisma=new PrismaClient({adapter:new PrismaNeon({connectionString:url})});try{await prisma.$queryRaw`SELECT 1`;const rows=await prisma.$queryRaw<Array<{migration_name:string}>>`SELECT migration_name FROM "_prisma_migrations" ORDER BY started_at DESC LIMIT 10`;console.info(`Banco acessível; ${rows.length} migrations recentes encontradas. Nenhuma migration aplicada.`)}finally{await prisma.$disconnect()}

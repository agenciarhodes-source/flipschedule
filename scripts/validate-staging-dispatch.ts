import {appendFileSync} from "node:fs";
export function validateStagingDispatch(env:Record<string,string|undefined>=process.env){if(env.INPUT_CONFIRMATION!=="MIGRATE_STAGING"||env.INPUT_BACKUP_CONFIRMED!=="true")throw new Error("STAGING_DISPATCH_DENIED");const changeId=env.INPUT_CHANGE_ID??"";if(changeId.length<1||changeId.length>80||!/^[A-Za-z0-9._/-]+$/.test(changeId)||changeId.includes("..")||changeId.startsWith("/")||changeId.endsWith("/")||/[\x00-\x1f\x7f]/.test(changeId))throw new Error("STAGING_CHANGE_ID_INVALID");return{changeId}}
if(import.meta.url===`file://${process.argv[1]}`)try{const {changeId}=validateStagingDispatch();if(process.env.GITHUB_OUTPUT)appendFileSync(process.env.GITHUB_OUTPUT,`change_id=${changeId}\n`)}catch{console.error("Dispatch de staging inválido.");process.exitCode=1}

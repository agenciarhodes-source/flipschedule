import { z } from "zod";
import { getPrismaClient } from "../lib/db";
import { normalizeEmail } from "../lib/auth/utils";
const schema=z.object({email:z.string().trim().email().transform(normalizeEmail),confirmation:z.literal("BOOTSTRAP_PLATFORM_OWNER")});
export async function bootstrapPlatformOwner(input:unknown,prisma=getPrismaClient()){const data=schema.parse(input);return prisma.$transaction(async tx=>{const user=await tx.user.findUnique({where:{emailNormalized:data.email},select:{id:true,status:true,emailVerified:true,emailVerifiedAt:true,platformOperator:{select:{id:true,role:true,status:true}}}});if(!user||user.status!=="ACTIVE"||(!user.emailVerified&&!user.emailVerifiedAt))throw new Error("PLATFORM_OWNER_BOOTSTRAP_DENIED");if(user.platformOperator){if(user.platformOperator.role==="PLATFORM_OWNER"&&user.platformOperator.status==="ACTIVE")return {created:false,operatorId:user.platformOperator.id};throw new Error("PLATFORM_OWNER_BOOTSTRAP_CONFLICT")}const operator=await tx.platformOperator.create({data:{userId:user.id,role:"PLATFORM_OWNER",status:"ACTIVE",createdByUserId:user.id}});await tx.auditLog.create({data:{actorUserId:user.id,action:"platform.operator.created",resourceType:"PlatformOperator",resourceId:operator.id,outcome:"SUCCESS"}});return {created:true,operatorId:operator.id}})}
async function main(){const result=await bootstrapPlatformOwner({email:process.env.PLATFORM_OWNER_EMAIL,confirmation:process.env.PLATFORM_OWNER_CONFIRMATION});console.info(JSON.stringify(result))}
if(import.meta.url===`file://${process.argv[1]}`)main().catch(()=>{console.error("Bootstrap de operador da plataforma falhou.");process.exitCode=1})

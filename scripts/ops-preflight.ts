export {};
const env=process.env,environment=env.APP_ENV??env.NODE_ENV??"development",issues:string[]=[];const origin=env.PUBLIC_APP_ORIGIN??env.BETTER_AUTH_URL;if(origin){try{const url=new URL(origin);if(environment==="production"&&url.protocol!=="https:")issues.push("PUBLIC_ORIGIN_INVALID")}catch{issues.push("PUBLIC_ORIGIN_INVALID")}}else if(environment==="production")issues.push("PUBLIC_ORIGIN_MISSING");if(environment==="production"){for(const key of ["DATABASE_URL","BETTER_AUTH_SECRET","RATE_LIMIT_HASH_KEY"] as const)if(!env[key]?.trim())issues.push(`${key}_MISSING`);if(env.ASAAS_BASE_URL?.includes("sandbox"))issues.push("ASAAS_ENVIRONMENT_MIXED")}if(env.OPERATIONAL_MODE&&!new Set(["NORMAL","READ_ONLY","MAINTENANCE"]).has(env.OPERATIONAL_MODE))issues.push("OPERATIONAL_MODE_INVALID");if(env.PILOT_MODE==="true"&&!env.PILOT_TENANT_SLUGS?.trim())issues.push("PILOT_ALLOWLIST_EMPTY");if(issues.length){console.error(`Preflight bloqueado: ${issues.join(", ")}`);process.exitCode=1}else console.info(`Preflight válido para ${environment}; nenhuma conexão externa foi realizada.`);

export {};const raw=process.env.BASE_URL;if(!raw)throw new Error("BASE_URL_REQUIRED");const base=new URL(raw);if(base.hostname!=="localhost"&&base.hostname!=="127.0.0.1"&&process.env.ALLOW_PREVIEW_SMOKE!=="true")throw new Error("SMOKE_TARGET_DENIED");if(process.env.APP_ENV==="production"&&!process.argv.includes("--allow-production"))throw new Error("PRODUCTION_SMOKE_DENIED");for(const path of ["/api/health/live","/api/health/ready","/login","/"]){const response=await fetch(new URL(path,base),{redirect:"manual",headers:{"user-agent":"FlipSchedule controlled smoke"}});if(response.status>=500)throw new Error(`SMOKE_FAILED_${path}`);console.info(`${path}: ${response.status}`)}
